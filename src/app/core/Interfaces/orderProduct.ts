// Exporting an interface named OrderProduct, which can be used in other parts of the application.
export interface OrderProduct {
  // Property to hold the unique identifier of the product, represented as a number.
  ProductId: number;

  // Property to hold the quantity of the product ordered, also represented as a number.
  Quantity: number;
}
